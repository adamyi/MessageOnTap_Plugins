<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cmu.chimps.iamhome">

    <application android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true" android:theme="@style/AppTheme">
        <!-- TODO: move the extension along with title&description to a service and use activity only for settings -->

        <service android:name=".SaveHomeWifiService"
            android:exported="false"/>
        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".IAmHomeSettingsActivity"
            android:label="@string/extension_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".IAmHomePlugin"
            android:label="@string/extension_title">
            <intent-filter>
                <action android:name="edu.cmu.chimps.messageontap_prototype.Plugin" />
            </intent-filter>
            <meta-data android:name="description"
                android:value="@string/extension_description" />
            <meta-data android:name="settingsActivity"
                android:value=".IAmHomeSettingsActivity" />
        </service>

    </application>

</manifest>